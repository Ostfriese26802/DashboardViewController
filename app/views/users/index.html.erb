<p id="notice"><%= notice %></p>

<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>Username</th>
     
      <th>Gruppe</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.username %></td>
        
        <td><%= Gruppe.find(user.gruppe_id).name %></td>
        <td><%= link_to 'Anzeigen', user %></td>
        <td><%= link_to 'bearbeiten', edit_user_path(user) %></td>
        <td><%= link_to 'löschen', user, method: :delete, data: { confirm: 'Sind Sie sicher?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Neuer Benutzer', new_user_path %>
